# Gym Reminder Bot 💪

یک ربات تلگرامی برای مدیریت و پیگیری برنامه‌های ورزشی با قابلیت یادآوری و تایمر استراحت

## ویژگی‌ها

- ✅ ایجاد برنامه‌های ورزشی برای روزهای مختلف هفته
- ✅ افزودن حرکات ورزشی با مشخصات (نام، تعداد ست، وزنه)
- ✅ شروع تمرین با راهنمایی گام‌به‌گام
- ✅ تایمر استراحت 1 دقیقه‌ای بین حرکات
- ✅ آلارم پایان زمان استراحت
- ✅ رابط کاربری فارسی

## نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8 یا بالاتر
- یک توکن ربات تلگرام (از [@BotFather](https://t.me/botfather) دریافت کنید)

### مراحل نصب

1. کلون کردن مخزن:
```bash
git clone https://github.com/rexagamer/gym-reminder-bot.git
cd gym-reminder-bot
```

2. نصب وابستگی‌ها:
```bash
pip install -r requirements.txt
```

3. ایجاد فایل `.env`:
```bash
cp .env.example .env
```

4. ویرایش فایل `.env` و اضافه کردن توکن ربات:
```
TELEGRAM_BOT_TOKEN=your_actual_bot_token_here
```

5. اجرای ربات:
```bash
python bot.py
```

## نحوه استفاده

### دستورات موجود

- `/start` - شروع کار با ربات
- `/newprogram` - ساخت برنامه ورزشی جدید
- `/myprograms` - مشاهده برنامه‌های ذخیره شده
- `/start_workout` - شروع یک جلسه تمرین
- `/help` - نمایش راهنما
- `/cancel` - لغو عملیات جاری

### مثال استفاده

#### 1. ساخت برنامه ورزشی

1. دستور `/newprogram` را ارسال کنید
2. روز هفته مورد نظر را انتخاب کنید (مثلاً شنبه)
3. حرکات را به این صورت اضافه کنید:
   ```
   پرس سینه، 4، 60
   زیر بغل، 3، 50
   جلو بازو، 3، 15
   ```
4. بعد از اضافه کردن همه حرکات، کلمه `تمام` را بنویسید

#### 2. شروع تمرین

1. دستور `/start_workout` را ارسال کنید
2. برنامه مورد نظر را انتخاب کنید
3. هر حرکت را انجام دهید و دکمه "انجام شد" را بزنید
4. بین هر حرکت، 1 دقیقه استراحت کنید
5. بعد از آلارم، حرکت بعدی نمایش داده می‌شود

## ساختار پروژه

```
gym-reminder-bot/
├── bot.py              # فایل اصلی ربات
├── database.py         # مدیریت پایگاه داده
├── requirements.txt    # وابستگی‌های Python
├── .env.example        # نمونه فایل تنظیمات
├── .gitignore         # فایل‌های نادیده گرفته شده توسط Git
└── README.md          # این فایل
```

## فناوری‌های استفاده شده

- **Python 3.8+**: زبان برنامه‌نویسی اصلی
- **python-telegram-bot**: کتابخانه ربات تلگرام
- **SQLite**: پایگاه داده برای ذخیره برنامه‌ها و تمرینات
- **asyncio**: برای مدیریت تایمرها و عملیات غیرهمزمان

## مشارکت

هرگونه پیشنهاد و مشارکت در بهبود این پروژه خوشایند است. لطفاً:

1. یک Fork از مخزن بگیرید
2. تغییرات خود را اعمال کنید
3. یک Pull Request ارسال کنید

## مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## پشتیبانی

اگر سوال یا مشکلی دارید، لطفاً یک Issue ایجاد کنید.